"""Business services — class.verb pattern.

Verb vocabulary (locked):
    create   construct new + persist
    fetch    get one by id
    list     get many with filters
    update   modify by id
    delete   remove by id
    search   rank by similarity/relevance
    parse    transform input format
    analyze  compute over a dataset
    execute  run main operation (catch-all)
"""
